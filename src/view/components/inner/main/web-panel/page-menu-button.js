var React = require('react');

var PageMenuButton = React.createClass({
    render: function () {
        return (
            <div className="page-menu-button">

            </div>
        );
    }
});

module.exports = PageMenuButton;